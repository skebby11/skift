<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Template for an optional, gitignored `Skift/Secrets.plist` that turns a
  build into "login-only": pre-provisioned Strava credentials instead of the
  default BYO fields in Settings (see docs/strava-upload.md "Provisioned
  builds"). Copy this file to `Skift/Secrets.plist` and fill in your own
  Strava API app's Client ID/Secret (strava.com/settings/api); XcodeGen
  bundles it automatically on the next `xcodegen generate`. Never commit the
  real file — `Skift/Secrets.plist` is in .gitignore.
-->
<plist version="1.0">
<dict>
    <key>StravaClientID</key>
    <string>YOUR_STRAVA_CLIENT_ID</string>
    <key>StravaClientSecret</key>
    <string>YOUR_STRAVA_CLIENT_SECRET</string>
</dict>
</plist>
